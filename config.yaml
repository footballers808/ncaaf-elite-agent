# Injury knobs
injury_spread_weight_per_point: 0.45   # converts injury burden diff to spread points
injury_total_weight_per_point:  0.20   # converts combined burden to total points
injury_decay_days: 28                  # older reports decayed

# Matchup knobs
matchup_spread_scale: 0.03
matchup_total_scale:  0.03

# Global caps (safety)
max_spread_adj_component: 3.0
max_total_adj_component:  6.0
